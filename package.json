{
  "name": "graphql-relay-app",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "biome format --write",
    "lint": "turbo run lint",
    "tc": "turbo run tc"
  },
  "devDependencies": {
			"turbo": "^2.5.8"
		},
		"packageManager": "pnpm@10.15.1",
		"engines": {
			"node": ">= 23"
		}
}
